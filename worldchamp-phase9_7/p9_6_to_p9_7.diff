--- /mnt/data/worldchamp-phase9_6/pine/worldchamp.pine	2026-08-17 08:28:34.686058525 +0000
+++ /mnt/data/worldchamp-phase9_7/pine/worldchamp.pine	2026-08-17 08:29:52.334260673 +0000
@@ -1,8 +1,8 @@
 //@version=6
-indicator("WorldChamp - Phase 9.6 Developing 1H Impulse Anchor", shorttitle="WorldChamp P9.6", overlay=true, max_lines_count=350, max_boxes_count=100, max_labels_count=500, max_bars_back=1500)
+indicator("WorldChamp - Phase 9.7 Anchor Retirement + Fresh Re-arm", shorttitle="WorldChamp P9.7", overlay=true, max_lines_count=350, max_boxes_count=100, max_labels_count=500, max_bars_back=1500)
 
 // =============================================================================
-// WORLDCHAMP - PHASE 9.6: DEVELOPING 1H IMPULSE ANCHOR
+// WORLDCHAMP - PHASE 9.7: ANCHOR RETIREMENT + FRESH RE-ARM
 // Manual execution only. No strategy orders are generated by this script.
 //
 // Includes all validated Phase 3 modules:
@@ -1174,7 +1174,7 @@
     array.shift(pivot15Prices)
     array.shift(pivot15Types)
 
-// Phase 9.6 - developing 1H impulse anchor.
+// Phase 9.7 - developing 1H impulse anchor + retirement lifecycle.
 //
 // P9.5 showed that the legacy anchor frequently arrived already beyond .886
 // (SEL-BR1) because it waited for a completed directional 1H swing pair. That
@@ -1188,13 +1188,32 @@
 // This does not loosen .886, value-side, range-side, or 15M swing rules. The
 // endpoint only refreshes when price genuinely extends the impulse. Historical
 // scanning is backward-only, so no future bars are used.
+// P9.7 anchor lifecycle. A 1H origin that has breached .886 is retired and
+// cannot reactivate merely because price later returns above the level. The
+// retired side re-arms only after TradingView confirms a genuinely newer 1H
+// opposite swing origin. Long/short retirement is tracked independently.
+var int retiredLongOriginTime = na
+var int retiredShortOriginTime = na
+
+bool longOriginRearmedThisBar = not na(retiredLongOriginTime) and not na(lastLowTime1h) and lastLowTime1h > retiredLongOriginTime
+bool shortOriginRearmedThisBar = not na(retiredShortOriginTime) and not na(lastHighTime1h) and lastHighTime1h > retiredShortOriginTime
+bool originRearmedThisBar = longOriginRearmedThisBar or shortOriginRearmedThisBar
+if longOriginRearmedThisBar
+    retiredLongOriginTime := na
+if shortOriginRearmedThisBar
+    retiredShortOriginTime := na
+
+bool longOriginRetired = locationDirection == LOC_LONG and not na(lastLowTime1h) and not na(retiredLongOriginTime) and lastLowTime1h <= retiredLongOriginTime
+bool shortOriginRetired = locationDirection == LOC_SHORT and not na(lastHighTime1h) and not na(retiredShortOriginTime) and lastHighTime1h <= retiredShortOriginTime
+bool locationOriginRetired = longOriginRetired or shortOriginRetired
+
 float baseAnchorStartPrice = na
 float baseAnchorEndPrice = na
 int baseAnchorStartTime = na
 int baseAnchorEndTime = na
 int developingAnchorScanBars = 600  // 50 chart-hours on 5M; safely covers the audited anchor ages.
 
-if locationDirection == LOC_LONG and not na(lastLow1h) and not na(lastLowTime1h)
+if locationDirection == LOC_LONG and not longOriginRetired and not na(lastLow1h) and not na(lastLowTime1h)
     baseAnchorStartPrice := lastLow1h
     baseAnchorStartTime := lastLowTime1h
     float developingHigh = na
@@ -1206,7 +1225,7 @@
                 developingHighTime := time[i]
     baseAnchorEndPrice := developingHigh
     baseAnchorEndTime := developingHighTime
-else if locationDirection == LOC_SHORT and not na(lastHigh1h) and not na(lastHighTime1h)
+else if locationDirection == LOC_SHORT and not shortOriginRetired and not na(lastHigh1h) and not na(lastHighTime1h)
     baseAnchorStartPrice := lastHigh1h
     baseAnchorStartTime := lastHighTime1h
     float developingLow = na
@@ -1330,6 +1349,20 @@
     else if locationDirection == LOC_SHORT and close > fib886 + locationTolerance
         locationInvalidated := true
 
+// Retire the confirmed 1H origin on the first bar that invalidates its active
+// developing impulse. This also catches an anchor that is already beyond .886
+// when first selected. The retirement takes effect on the next calculation; the
+// current bar remains explicitly INVALIDATED and downstream gates stay closed.
+bool locationInvalidatedNew = locationInvalidated and (anchorChanged or not anchorWasInvalidated)
+bool originRetiredThisBar = false
+if locationInvalidatedNew and not na(baseAnchorStartTime)
+    if locationDirection == LOC_LONG
+        retiredLongOriginTime := na(retiredLongOriginTime) ? baseAnchorStartTime : math.max(retiredLongOriginTime, baseAnchorStartTime)
+        originRetiredThisBar := true
+    else if locationDirection == LOC_SHORT
+        retiredShortOriginTime := na(retiredShortOriginTime) ? baseAnchorStartTime : math.max(retiredShortOriginTime, baseAnchorStartTime)
+        originRetiredThisBar := true
+
 // Audit event: count an NY invalidation only when price actually crosses the
 // selected .886 boundary on this bar. Merely selecting an old anchor while price
 // is already beyond .886 is NOT counted as an NY crossing; PRE-BR handles that.
@@ -1346,7 +1379,8 @@
 f_locationState() =>
     string result = "CONTEXT BLOCKED"
     if locationDirection != LOC_NONE
-        result := not baseLegValid ? "LEG WARMING" :
+        result := locationOriginRetired ? "WAIT NEW 1H IMPULSE · RETIRED" :
+          not baseLegValid ? "LEG WARMING" :
           not anchorLegValid ? "ANCHOR INVALID" :
           locationInvalidated ? (locationDirection == LOC_LONG ? "INVALIDATED < .886" : "INVALIDATED > .886") :
           not hasValueMap ? "NO VALUE MAP" :
@@ -1360,7 +1394,7 @@
 string locationState = f_locationState()
 
 // -----------------------------------------------------------------------------
-// Phase 9.6 - location geometry + developing-anchor audit (Validation only)
+// Phase 9.7 - location geometry + anchor-lifecycle audit (Validation only)
 //
 // Diagnostics only. These values NEVER alter production location logic. The
 // audit answers whether the selected impulse was already stale before NY,
@@ -1410,6 +1444,9 @@
 var int valAuditAllGeometryPass = 0
 var int valAuditZoneTouchAny = 0
 var int valAuditSelectedBreached = 0
+var int valAuditRetireEvents = 0
+var int valAuditRearmEvents = 0
+var int valLocRetired = 0
 
 // -----------------------------------------------------------------------------
 // Phase 9.5 - full-funnel historical validation counters
@@ -1469,11 +1506,18 @@
     valAuditAllGeometryPass := 0
     valAuditZoneTouchAny := 0
     valAuditSelectedBreached := 0
+    valAuditRetireEvents := 0
+    valAuditRearmEvents := 0
+    valLocRetired := 0
 
 if barstate.isconfirmed and inNyActiveSession
     bool valContextPassNow = autoLocationDirection != LOC_NONE
     if valContextPassNow
         valCtxPass += 1
+        if originRetiredThisBar
+            valAuditRetireEvents += 1
+        if originRearmedThisBar
+            valAuditRearmEvents += 1
 
         // Snapshot the first usable anchor, then refresh the audit snapshot only
         // when production selects a different anchor. Initial selection is not
@@ -1527,7 +1571,9 @@
             valAuditRawTouches += 1
 
         // Exclusive location stop reason, matching f_locationState() order.
-        if not baseLegValid or not anchorLegValid
+        if locationOriginRetired
+            valLocRetired += 1
+        else if not baseLegValid or not anchorLegValid
             valLocLegBlocked += 1
         else if locationInvalidated
             valLocInvalidated += 1
@@ -1919,8 +1965,8 @@
     string auditAge = na(valAuditAnchorAgeHours) ? "n/a" : str.tostring(valAuditAnchorAgeHours, "#.0") + "h"
     string auditPre = valAuditPreNyBreach ? "Y" : "N"
     string valLine1 = "WC VAL · " + str.tostring(valBars) + "B | CTX P" + str.tostring(valCtxPass) + " B" + str.tostring(valCtxBlocked) + " | LOC V" + str.tostring(valLocValid) + " Z" + str.tostring(valLocInZone) + " A" + str.tostring(valLocApproach)
-    string valLine2 = "BLK LEG" + str.tostring(valLocLegBlocked) + " INV" + str.tostring(valLocInvalidated) + " MAP" + str.tostring(valLocNoValue) + " RNG" + str.tostring(valLocWrongRange) + " VAL" + str.tostring(valLocWrongValueSide) + " SW" + str.tostring(valLocNoSwing)
-    string valLine3 = "ANCH " + auditDir + " " + valAuditAnchorSource + " AGE " + auditAge + " CHG" + str.tostring(valAuditAnchorChanges) + " | PRE-BR " + auditPre + " SEL-BR" + str.tostring(valAuditSelectedBreached) + " NY-INV" + str.tostring(valAuditNyInvalidationEvents)
+    string valLine2 = "BLK RET" + str.tostring(valLocRetired) + " LEG" + str.tostring(valLocLegBlocked) + " INV" + str.tostring(valLocInvalidated) + " MAP" + str.tostring(valLocNoValue) + " RNG" + str.tostring(valLocWrongRange) + " VAL" + str.tostring(valLocWrongValueSide) + " SW" + str.tostring(valLocNoSwing)
+    string valLine3 = "ANCH " + auditDir + " " + valAuditAnchorSource + " AGE " + auditAge + " CHG" + str.tostring(valAuditAnchorChanges) + " | PRE-BR " + auditPre + " SEL-BR" + str.tostring(valAuditSelectedBreached) + " NY-INV" + str.tostring(valAuditNyInvalidationEvents) + " RET" + str.tostring(valAuditRetireEvents) + " RE" + str.tostring(valAuditRearmEvents)
     string valLine4 = "GEO L" + str.tostring(valAuditLegPass) + " R" + str.tostring(valAuditRangePass) + " V" + str.tostring(valAuditValuePass) + " S" + str.tostring(valAuditSwingPass) + " A" + str.tostring(valAuditAllGeometryPass) + " T" + str.tostring(valAuditZoneTouchAny)
     string valLine5 = "RAW E" + str.tostring(valAuditRawEligible) + " Z" + str.tostring(valAuditRawTouches) + " | FINAL V" + str.tostring(valLocValid) + " | NYO " + f_auditPrice(valAuditNyOpen)
     string valLine6 = ".705 " + f_auditPrice(valAuditFib705) + " | .788 " + f_auditPrice(valAuditFib788) + " | .886 " + f_auditPrice(valAuditFib886)
@@ -2246,7 +2292,7 @@
   not aggressionDetected ? "WAIT · AGGRESSION" :
   absorptionDetected ? (sellerAbsorption ? "ABSORPTION · SELLERS" : "ABSORPTION · BUYERS") :
   potentialAbsorption ? "WATCH · POTENTIAL ABS" : "AGGRESSION · RESULT OK"
-string masterState = setupDiagnosticMode ? "P9.6 · SETUP DIAG" : "P9.6 · " + str.upper(operatingModeInput) + " · MANUAL"
+string masterState = setupDiagnosticMode ? "P9.7 · SETUP DIAG" : "P9.7 · " + str.upper(operatingModeInput) + " · MANUAL"
 
 // -----------------------------------------------------------------------------
 // Clean chart visuals
@@ -2628,11 +2674,12 @@
     str.contains(locationState, "IN LONG") ? color.lime :
      str.contains(locationState, "IN SHORT") ? color.red :
      str.contains(locationState, "APPROACHING") ? color.aqua :
+     str.contains(locationState, "RETIRED") ? color.orange :
      str.contains(locationState, "INVALID") or str.contains(locationState, "WRONG") or str.contains(locationState, "OVERLAPS") ? color.red :
      str.contains(locationState, "VALID") ? color.lime : color.silver
 
 f_anchorText() =>
-    anchorLegValid ? anchorSource + " · " + f_price(anchorStartPrice) + " → " + f_price(anchorEndPrice) : "n/a"
+    locationOriginRetired ? "RETIRED · WAIT NEW 1H" : anchorLegValid ? anchorSource + " · " + f_price(anchorStartPrice) + " → " + f_price(anchorEndPrice) : "n/a"
 
 f_zoneText() =>
     anchorLegValid ? f_price(locationZoneTop) + " → " + f_price(locationZoneBottom) : "n/a"
@@ -2788,7 +2835,7 @@
         table.clear(dash, 0, 0, 1, 20)
 
         table.cell(dash, 0, 0, "WORLDCHAMP", text_color=color.white, text_size=size.small, bgcolor=color.new(color.silver, 82))
-        table.cell(dash, 1, 0, "P9.6 · " + f_modeText(), text_color=f_modeColor(), text_size=size.small, bgcolor=color.new(color.silver, 82))
+        table.cell(dash, 1, 0, "P9.7 · " + f_modeText(), text_color=f_modeColor(), text_size=size.small, bgcolor=color.new(color.silver, 82))
 
         table.cell(dash, 0, 1, "Chart / Session", text_size=size.tiny)
         table.cell(dash, 1, 1, f_tfStatus(isFiveMinute) + " · " + f_sessionStatus() + " · " + (useTestWindow ? str.tostring(testDays) + "D" : "FULL"), text_color=isFiveMinute ? (inNyActiveSession ? color.lime : color.silver) : color.red, text_size=size.tiny)
