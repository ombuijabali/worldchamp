--- /mnt/data/worldchamp-phase9_3_1/pine/worldchamp.pine	2026-08-16 23:43:34.801966746 +0000
+++ /mnt/data/worldchamp-phase9_4/pine/worldchamp.pine	2026-08-16 23:51:09.348210200 +0000
@@ -1,8 +1,8 @@
 //@version=6
-indicator("WorldChamp - Phase 9.3.1 Production", shorttitle="WorldChamp P9.3.1", overlay=true, max_lines_count=350, max_boxes_count=100, max_labels_count=500)
+indicator("WorldChamp - Phase 9.4 Location Anchor Audit", shorttitle="WorldChamp P9.4", overlay=true, max_lines_count=350, max_boxes_count=100, max_labels_count=500, max_bars_back=1500)
 
 // =============================================================================
-// WORLDCHAMP - PHASE 9.3.1: FULL-FUNNEL VALIDATION COUNTERS
+// WORLDCHAMP - PHASE 9.4: LOCATION ANCHOR AUDIT
 // Manual execution only. No strategy orders are generated by this script.
 //
 // Includes all validated Phase 3 modules:
@@ -74,6 +74,7 @@
 // Constants
 // -----------------------------------------------------------------------------
 string WC_TZ = "America/New_York"
+int MS_HOUR = 60 * 60 * 1000
 string TF_4H = "240"
 string TF_1H = "60"
 string TF_15M = "15"
@@ -278,12 +279,12 @@
 bool isFiveMinute = timeframe.isminutes and timeframe.multiplier == 5
 
 if require5m and not isFiveMinute
-    runtime.error("WorldChamp Phase 9.3.1 must run on a 5-minute chart while 'Require 5-minute chart' is enabled.")
+    runtime.error("WorldChamp Phase 9.4 must run on a 5-minute chart while 'Require 5-minute chart' is enabled.")
 
 float chartSeconds = timeframe.in_seconds()
 float profileLtfSeconds = timeframe.in_seconds(profileLtf)
 if profileLtfSeconds >= chartSeconds
-    runtime.error("WorldChamp Phase 9.3.1 requires the profile lower timeframe to be below the chart timeframe.")
+    runtime.error("WorldChamp Phase 9.4 requires the profile lower timeframe to be below the chart timeframe.")
 
 // -----------------------------------------------------------------------------
 // Development window
@@ -1290,6 +1291,7 @@
 var bool locationInvalidated = false
 
 bool anchorChanged = locationDirection != trackedLocationDirection or nz(anchorStartTime, 0) != nz(trackedAnchorStartTime, 0) or nz(anchorEndTime, 0) != nz(trackedAnchorEndTime, 0)
+bool anchorWasInvalidated = locationInvalidated
 if anchorChanged
     trackedLocationDirection := locationDirection
     trackedAnchorStartTime := anchorStartTime
@@ -1302,6 +1304,12 @@
     else if locationDirection == LOC_SHORT and close > fib886 + locationTolerance
         locationInvalidated := true
 
+// Audit event: count an NY invalidation only when price actually crosses the
+// selected .886 boundary on this bar. Merely selecting an old anchor while price
+// is already beyond .886 is NOT counted as an NY crossing; PRE-BR handles that.
+bool crossed886ThisBar = anchorLegValid and bar_index > 0 and (locationDirection == LOC_LONG ? (close < fib886 - locationTolerance and close[1] >= fib886 - locationTolerance) : locationDirection == LOC_SHORT ? (close > fib886 + locationTolerance and close[1] <= fib886 + locationTolerance) : false)
+bool locationInvalidatedThisBar = locationInvalidated and (anchorChanged or not anchorWasInvalidated) and crossed886ThisBar
+
 bool candleTouchesLocation = locationEligible and high >= locationZoneBottom and low <= locationZoneTop
 float approachDistance = anchorLegValid ? impulseRange * approachPct / 100.0 : na
 bool approachingLocation = locationEligible and not candleTouchesLocation and not locationInvalidated and (locationDirection == LOC_LONG ? close > locationZoneTop and close <= locationZoneTop + approachDistance : locationDirection == LOC_SHORT ? close < locationZoneBottom and close >= locationZoneBottom - approachDistance : false)
@@ -1326,7 +1334,52 @@
 string locationState = f_locationState()
 
 // -----------------------------------------------------------------------------
-// Phase 9.3 - full-funnel historical validation counters
+// Phase 9.4 - location anchor audit (Validation only)
+//
+// Diagnostics only. These values NEVER alter production location logic. The
+// audit answers whether the selected impulse was already stale before NY,
+// whether it invalidated during NY, whether a fresh anchor replaced it, and
+// whether the rest of the location geometry would have qualified if the
+// persistent invalidation flag were ignored.
+// -----------------------------------------------------------------------------
+f_auditPrice(float value) =>
+    na(value) ? "n/a" : str.tostring(value, format.mintick)
+
+f_breachedBeforeNy(int impulseEndTime, float invalidationLevel, int direction, int nyStartTime, float tolerance) =>
+    bool breached = false
+    if not na(impulseEndTime) and not na(invalidationLevel) and not na(nyStartTime) and impulseEndTime < nyStartTime
+        for i = 1 to 1200
+            if i <= bar_index
+                int t = time[i]
+                if t < impulseEndTime
+                    break
+                if t >= impulseEndTime and t < nyStartTime
+                    if direction == LOC_LONG and close[i] < invalidationLevel - tolerance
+                        breached := true
+                        break
+                    else if direction == LOC_SHORT and close[i] > invalidationLevel + tolerance
+                        breached := true
+                        break
+    breached
+
+var int valAuditNyStartTime = na
+var float valAuditNyOpen = na
+var bool valAuditAnchorSeen = false
+var int valAuditDirection = LOC_NONE
+var string valAuditAnchorSource = "n/a"
+var int valAuditAnchorEndTime = na
+var float valAuditAnchorAgeHours = na
+var float valAuditFib705 = na
+var float valAuditFib788 = na
+var float valAuditFib886 = na
+var bool valAuditPreNyBreach = false
+var int valAuditAnchorChanges = 0
+var int valAuditNyInvalidationEvents = 0
+var int valAuditRawEligible = 0
+var int valAuditRawTouches = 0
+
+// -----------------------------------------------------------------------------
+// Phase 9.4 - full-funnel historical validation counters
 // Validation mode uses production rules. These counters do not gate or alter
 // trading logic; they only explain where each completed NY session stopped.
 // Context PASS means the production environment resolved to a directional
@@ -1361,12 +1414,57 @@
     valLocNoSwing := 0
     valParticipationPass := 0
     valOrderflowReady := 0
+    valAuditNyStartTime := time
+    valAuditNyOpen := open
+    valAuditAnchorSeen := false
+    valAuditDirection := LOC_NONE
+    valAuditAnchorSource := "n/a"
+    valAuditAnchorEndTime := na
+    valAuditAnchorAgeHours := na
+    valAuditFib705 := na
+    valAuditFib788 := na
+    valAuditFib886 := na
+    valAuditPreNyBreach := false
+    valAuditAnchorChanges := 0
+    valAuditNyInvalidationEvents := 0
+    valAuditRawEligible := 0
+    valAuditRawTouches := 0
 
 if barstate.isconfirmed and inNyActiveSession
     bool valContextPassNow = autoLocationDirection != LOC_NONE
     if valContextPassNow
         valCtxPass += 1
 
+        // Snapshot the first usable anchor, then refresh the audit snapshot only
+        // when production selects a different anchor. Initial selection is not
+        // counted as an anchor change; later replacements are.
+        if anchorLegValid and (not valAuditAnchorSeen or anchorChanged)
+            if valAuditAnchorSeen and anchorChanged
+                valAuditAnchorChanges += 1
+            valAuditAnchorSeen := true
+            valAuditDirection := locationDirection
+            valAuditAnchorSource := anchorSource
+            valAuditAnchorEndTime := anchorEndTime
+            valAuditAnchorAgeHours := not na(anchorEndTime) ? math.max(0.0, (time - anchorEndTime) / float(MS_HOUR)) : na
+            valAuditFib705 := fib705
+            valAuditFib788 := fib788
+            valAuditFib886 := fib886
+            bool auditPreBreachNow = f_breachedBeforeNy(anchorEndTime, fib886, locationDirection, valAuditNyStartTime, locationTolerance)
+            valAuditPreNyBreach := valAuditPreNyBreach or auditPreBreachNow
+
+        if locationInvalidatedThisBar
+            valAuditNyInvalidationEvents += 1
+
+        // RAW means all production geometry checks except the persistent .886
+        // invalidation flag. It tells us whether stale invalidation alone is the
+        // blocker or whether the anchor/value/swing geometry also fails.
+        bool auditRawEligibleNow = locationEligible
+        bool auditRawTouchNow = auditRawEligibleNow and high >= locationZoneBottom and low <= locationZoneTop
+        if auditRawEligibleNow
+            valAuditRawEligible += 1
+        if auditRawTouchNow
+            valAuditRawTouches += 1
+
         // Exclusive location stop reason, matching f_locationState() order.
         if not baseLegValid or not anchorLegValid
             valLocLegBlocked += 1
@@ -1752,16 +1850,22 @@
     string setupSummary = setupPrefix + "  Arm" + str.tostring(setupSessionArmed) + "  Ret" + str.tostring(setupSessionRetest) + "  SF" + str.tostring(setupSessionSecondFailure) + "  L" + str.tostring(setupSessionReadyLong) + "  S" + str.tostring(setupSessionReadyShort) + "  Exp" + str.tostring(setupSessionExpired) + (setupTestMode ? "  TEST" : "")
     label.new(bar_index - 1, low[1], setupSummary, xloc=xloc.bar_index, yloc=yloc.belowbar, style=label.style_label_up, color=color.new(color.black, 60), textcolor=setupTestMode ? color.orange : color.white, size=size.tiny)
 
-// Phase 9.3: one full-funnel session summary in Validation mode. Individual
-// module summaries remain available only in Debug mode.
+// Phase 9.4: one full-funnel + location-anchor audit summary in Validation
+// mode. Individual module summaries remain available only in Debug mode.
 if nySessionEnded and validationMode and inTestWindow
     int valBars = valCtxPass + valCtxBlocked
+    string auditDir = valAuditDirection == LOC_LONG ? "L" : valAuditDirection == LOC_SHORT ? "S" : "-"
+    string auditAge = na(valAuditAnchorAgeHours) ? "n/a" : str.tostring(valAuditAnchorAgeHours, "#.0") + "h"
+    string auditPre = valAuditPreNyBreach ? "Y" : "N"
     string valLine1 = "WC VAL · " + str.tostring(valBars) + "B | CTX P" + str.tostring(valCtxPass) + " B" + str.tostring(valCtxBlocked) + " | LOC V" + str.tostring(valLocValid) + " Z" + str.tostring(valLocInZone) + " A" + str.tostring(valLocApproach)
     string valLine2 = "BLK LEG" + str.tostring(valLocLegBlocked) + " INV" + str.tostring(valLocInvalidated) + " MAP" + str.tostring(valLocNoValue) + " RNG" + str.tostring(valLocWrongRange) + " VAL" + str.tostring(valLocWrongValueSide) + " SW" + str.tostring(valLocNoSwing)
-    string valLine3 = "PART P" + str.tostring(valParticipationPass) + " H" + str.tostring(partSessionHigh) + " N" + str.tostring(partSessionNormal) + " L" + str.tostring(partSessionLow) + " | OF " + str.tostring(valOrderflowReady) + "/" + str.tostring(valBars) + " | AGG B" + str.tostring(aggSessionBuy) + " S" + str.tostring(aggSessionSell)
-    string valLine4 = "ABS SA" + str.tostring(absSessionSeller) + " BA" + str.tostring(absSessionBuyer) + " P" + str.tostring(absSessionPotential) + " | DOM B" + str.tostring(domSessionBuyer) + " S" + str.tostring(domSessionSeller) + " E" + str.tostring(domSessionExpired) + " | SET L" + str.tostring(setupSessionReadyLong) + " S" + str.tostring(setupSessionReadyShort)
-    string valSummary = valLine1 + "\n" + valLine2 + "\n" + valLine3 + "\n" + valLine4
-    label.new(bar_index - 1, high[1], valSummary, xloc=xloc.bar_index, yloc=yloc.abovebar, style=label.style_label_down, color=color.new(color.black, 48), textcolor=color.orange, size=size.tiny)
+    string valLine3 = "ANCH " + auditDir + " " + valAuditAnchorSource + " AGE " + auditAge + " CHG" + str.tostring(valAuditAnchorChanges) + " | PRE-BR " + auditPre + " NY-INV" + str.tostring(valAuditNyInvalidationEvents)
+    string valLine4 = "RAW E" + str.tostring(valAuditRawEligible) + " Z" + str.tostring(valAuditRawTouches) + " | FINAL V" + str.tostring(valLocValid) + " | NYO " + f_auditPrice(valAuditNyOpen)
+    string valLine5 = ".705 " + f_auditPrice(valAuditFib705) + " | .788 " + f_auditPrice(valAuditFib788) + " | .886 " + f_auditPrice(valAuditFib886)
+    string valLine6 = "PART P" + str.tostring(valParticipationPass) + " H" + str.tostring(partSessionHigh) + " N" + str.tostring(partSessionNormal) + " L" + str.tostring(partSessionLow) + " | OF " + str.tostring(valOrderflowReady) + "/" + str.tostring(valBars) + " | AGG B" + str.tostring(aggSessionBuy) + " S" + str.tostring(aggSessionSell)
+    string valLine7 = "ABS SA" + str.tostring(absSessionSeller) + " BA" + str.tostring(absSessionBuyer) + " P" + str.tostring(absSessionPotential) + " | DOM B" + str.tostring(domSessionBuyer) + " S" + str.tostring(domSessionSeller) + " E" + str.tostring(domSessionExpired) + " | SET L" + str.tostring(setupSessionReadyLong) + " S" + str.tostring(setupSessionReadyShort)
+    string valSummary = valLine1 + "\n" + valLine2 + "\n" + valLine3 + "\n" + valLine4 + "\n" + valLine5 + "\n" + valLine6 + "\n" + valLine7
+    label.new(bar_index - 1, high[1], valSummary, xloc=xloc.bar_index, yloc=yloc.abovebar, style=label.style_label_down, color=color.new(color.black, 44), textcolor=color.orange, size=size.tiny)
 
 // -----------------------------------------------------------------------------
 // Phase 7 - invalidation + structural target map
@@ -2080,7 +2184,7 @@
   not aggressionDetected ? "WAIT · AGGRESSION" :
   absorptionDetected ? (sellerAbsorption ? "ABSORPTION · SELLERS" : "ABSORPTION · BUYERS") :
   potentialAbsorption ? "WATCH · POTENTIAL ABS" : "AGGRESSION · RESULT OK"
-string masterState = setupDiagnosticMode ? "P9.3.1 · SETUP DIAG" : "P9.3.1 · " + str.upper(operatingModeInput) + " · MANUAL"
+string masterState = setupDiagnosticMode ? "P9.4 · SETUP DIAG" : "P9.4 · " + str.upper(operatingModeInput) + " · MANUAL"
 
 // -----------------------------------------------------------------------------
 // Clean chart visuals
@@ -2622,7 +2726,7 @@
         table.clear(dash, 0, 0, 1, 20)
 
         table.cell(dash, 0, 0, "WORLDCHAMP", text_color=color.white, text_size=size.small, bgcolor=color.new(color.silver, 82))
-        table.cell(dash, 1, 0, "P9.3.1 · " + f_modeText(), text_color=f_modeColor(), text_size=size.small, bgcolor=color.new(color.silver, 82))
+        table.cell(dash, 1, 0, "P9.4 · " + f_modeText(), text_color=f_modeColor(), text_size=size.small, bgcolor=color.new(color.silver, 82))
 
         table.cell(dash, 0, 1, "Chart / Session", text_size=size.tiny)
         table.cell(dash, 1, 1, f_tfStatus(isFiveMinute) + " · " + f_sessionStatus() + " · " + (useTestWindow ? str.tostring(testDays) + "D" : "FULL"), text_color=isFiveMinute ? (inNyActiveSession ? color.lime : color.silver) : color.red, text_size=size.tiny)
